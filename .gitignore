node_modules
dist
dist-temp/
dist-old/
.vercel

# ── Environment / secrets ──
.env
.env.*
!.env.example
.env.local
.env.*.local
.env*.local
.github-token
.github-push.sh

# ── OS / editor cruft ──
.DS_Store

# ── Supabase CLI local state ──
supabase/.temp/
supabase/ops/deploy-history.jsonl

# ── Local data files (never commit business data) ──
inventory_items.json

# ── Generated test results & capture diagnostics ──
public/test-results.json
public/test-results.json.raw
public/shots/nav-manifest.json
public/shots/capture-log.txt

# ── Local audit artifacts ──
audit/
